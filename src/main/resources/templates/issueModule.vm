$webResourceManager.requireResource("de.uhd.ifi.se.decision.management.jira:stylesheet-and-icon-resources")
$webResourceManager.requireResource("de.uhd.ifi.se.decision.management.jira:rest-client-resources")
$webResourceManager.requireResource("de.uhd.ifi.se.decision.management.jira:decision-knowledge-management-resources")
$webResourceManager.requireResource("de.uhd.ifi.se.decision.management.jira:issue-module-resources")
<div id="treant-container">
	<p>No Element has been selected</p>
</div>
#include("templates/dialog.vm")
<script>

    AJS.$(document).on("click","#downloadAsSomeTable",function(e){
        e.preventDefault();
        e.stopPropagation();
        console.log("clickOnTable")

        downloadMyJsonAsTable();

        return false;
    });
    AJS.$(document).off().on("click","#downloadLinkedElements",function(e){
        e.preventDefault();
        e.stopPropagation();
        console.log("click")
        downloadMyLinkedElements();

        return false;
    });

	fillIssueModule();
</script>